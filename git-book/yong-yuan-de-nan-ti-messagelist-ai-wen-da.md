# 永远的难题-MessageList AI 问答

## 起手

### 起手1

我在 使用 React 构建一个聊天前端页面，并且我希望可以对标 Telegram 等大厂的 Web 用户体验。 那么假设目前仅针对一个纯粹的聊天界面，可以发送消息（文本消息、图片消息、视频消息、语音消息等）、可以删除、撤回等。总之，把目前的页面简化为有一个可滚动的区域以及一个输入框。那么我在构建这个 ，可滚动的区域的React 组件的过程中，发现其中的消息处理，其实并没有我想象的这么简单，真的实现起来有很多的技术难点。对此，我自己总结了一些技术难点，但是在我告诉你之前，你能先说说对于一个专业的聊天窗口（不考虑输入框），使用React技术实现，需要考虑到哪些难点吗？因为我想到的可能不全面，并且我不想让你陷入我的思维定式。



对于你提到的技术难点，基本上是我在工作中发现的难点的父集。我很满意。继续，对于你提到的挑战，我们可以缩小一下范围：

1. 我们这里只讨论前端方面，因此不需要考虑数据一致性、连接等服务端或网络上的问题。
2. 不需要考虑虚拟列表，就像你说的，不同类型的消息（如图片）高度不同，动态计算困难，因此我们这里暂时不讨论，如果是直播消息列表场景，我会专门提供虚拟列表以支持简单的消息类型发送。
3. 我们这里只考虑我从前端工程师的角度，从在页面使用上的技术难点，因此不需要考虑安全、加密、监控调试等问题。

除此之外，我再补充几点，可能有重复：

1. 新消息进入窗口时如果触发滚动到底部，采用 smooth滑动，如果是图片，存在异步加载的问题，那么何时触发滚动到底部？
2. 如果是初次进入会话页面，我希望视窗直接在消息列表底部，此时图片、视频等异步上屏的内容又该怎么处理？（具体到实际场景：消息列表整体渲染 ->瞬间定位到底部 -> 图片、视频异步加载撑高了滚动列表 -> 视窗没有处于底部，和预期不符）

因此，请你重新总结一下技术难点，我也需要记录到本地。



### 起手2

我在 使用 React 构建一个聊天前端页面，并且我希望可以对标 Telegram 等大厂的 Web 用户体验。 那么假设目前仅针对一个纯粹的聊天界面，可以发送消息（文本消息、图片消息、视频消息、语音消息等）、可以删除、撤回等。总之，把目前的页面简化为有一个可滚动的区域以及一个输入框。那么我在构建这个 ，可滚动的区域的React 组件的过程中，发现其中的消息处理，其实并没有我想象的这么简单，真的实现起来有很多的技术难点。对此，我自己总结了一些技术难点，但是在我告诉你之前，你能先说说对于一个专业的聊天窗口（不考虑输入框），使用React技术实现，需要考虑到哪些难点吗？因为我想到的可能不全面，并且我不想让你陷入我的思维定式。

* 我们这里只讨论前端方面，因此不需要考虑数据一致性、连接等服务端或网络上的问题。
* 不需要考虑虚拟列表，不同类型的消息（如图片）高度不同，动态计算困难，因此我们这里暂时不讨论，如果是直播消息列表场景，我会专门提供虚拟列表以支持简单的消息类型发送。
* 我们这里只考虑我从前端工程师的角度，从在页面使用上的技术难点，因此不需要考虑安全、加密、监控调试等问题。

